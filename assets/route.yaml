apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: flask-todo
spec:
  host: flask-todo-openshift-flask-todo.apps.geo.rhtps.net
  port:
    targetPort: http
  to:
    kind: Service
    name: flask-todo
    weight: 100
status:
  ingress: []
